>>This run is almost identical to e7, with 1 exception.

>>The linear buoyancy profile has been strengthened by a factor of 4.

>>Go into the get-intial-temp.py file and change the
np.linspace(10,9.75,50) to np.linspace(10,9,50), corresponding 
to 10 deg at the top of domain and 9 deg at the bottom.

>>Change file save name to '...nf20.bin' from '...nf5.bin'
 
